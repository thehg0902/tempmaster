Maintain the Agency OS itself.

1. Run `python3 scripts/lint-skills.py` and fix every violation it reports.
2. Run `python3 scripts/generate-skill-registry.py` to refresh docs/REGISTRY.md.
3. Report a one-line summary per fixed violation.
