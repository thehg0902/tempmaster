# Video Prompt Construction (hero clips)
Target: 5-8s, single continuous motion, ends on a frame that works frozen
(hero-media freezes on last frame - prompt for a settling/resolving final
composition, not mid-action). Slow camera moves (push-in, drift) freeze
better than subject action. Same STYLE BLOCK as images.
Request the highest available resolution >= 1080p; re-encode per
hero-media mp4-vs-sequence.md after download.
One concept = one generation; iterate on the prompt in the log
(rejected rows kept) rather than burning credits on shotgun variants.
