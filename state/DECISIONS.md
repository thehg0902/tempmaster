# Decisions Log
<!-- Resolved ambiguities. Once logged here, a decision outranks skills
     and contracts (precedence level 3). Format:
     YYYY-MM-DD | topic | decision | decided-by (human/claude) | why -->

2026-07-20 | audience research methodology | HVAC is not one of the 11 niches in the audience-research library, so ran winners-writing-process fresh: live web research (Reddit, HomeStars, Trustpilot, RedFlagDeals) for verbatim customer language + a 6-competitor GTA top-player analysis, synthesized via a draft->adversarial-verify->revise workflow, result pasted into client.md Target Audience. | claude | operator asked for market research reflected in client.md; niche has no pre-built psychology library to draw from.
2026-07-20 | niche detection | Detected niche from GBP category line "HVAC contractor in Richmond Hill, Ontario": HVAC contractor, general local-service playbook, with AC installation & repair as the confirmed specialty emphasis (see Overrides > Services). Feeds Phase 2 niche-playbook selection. | claude | client-enrichment skill, GBP paste category line.
2026-07-20 | audience brief distillation (Phase 1) | Condensed from client.md Target Audience (full WWP) for downstream skills — avoid re-reading the full section: Avatar = homeowner 40s, Richmond Hill detached home, AC 20-25yrs old, either mid-emergency (heat wave, zero patience) or actively cross-shopping quotes on Reddit/HomeStars. Awareness=Solution-Aware(3), Sophistication=Stage4→5. Trust is the binding constraint (2/10) — not desire (8/10). Primary action = phone call, quote before a truck is dispatched. Top positioning angles: (1) true 5.0 rating framed plainly vs competitors' 4.7-4.8 averages, (2) AC install&repair claimed as identity not buried in generic "HVAC services", (3) hardened no-rental/no-commission/phone-quote-first policy answering Reliance/Enercare rental-trap fears, (4) 24-hour availability as concrete promise. Owner-operated angle exists but NOT usable until confirmed (state/QUESTIONS.md). Objections to pre-empt in copy: "am I being played on this quote", "will they push a rental on me", "will they upsell me", "will they actually show up on time". | claude | token economy — Phase 1-5 skills reference this instead of re-reading full Target Audience.
2026-07-20 | site architecture (Phase 1) | claude-proposed page map (no Pages override given). 4 pages: Home (index), AC Installation & Repair (dedicated specialty page — split out from the other 6 services specifically because it's the confirmed specialty emphasis AND the top-player research found "AC installation Richmond Hill" / "AC repair Richmond Hill" are the exact keywords the #1-ranking local competitor wins on), Services (the other 6: furnace install & maintenance, heat pump systems, ductwork, smart thermostat, humidifier, gas piping — bundled into one page per the "5+ services, otherwise one Services page" rule since none of the other 6 has confirmed distinct high-value search intent), Contact (physical address exists, rule 5). Nav (max 4 of 6 allowed): Home / AC Install & Repair / Services / Contact, phone number always visible in header (call-first niche). Primary conversion action = phone call to (416) 890-8550, derived from client.md Target Audience (Overrides Primary action was empty); quote-request form is secondary everywhere. Section order on Home is objection-led per the audience brief: hero (AC specialty + 24hr + rating) -> trust band (rating/reviews, custom section per component-api's "custom sections allowed") -> about ("why choose us" — no-rental/no-commission/phone-quote-first, answers the market's #1 trust objection before any hard CTA) -> services grid -> testimonials -> faq (pre-empts quote-fairness/rental/upsell/punctuality objections) -> cta -> contact teaser -> footer. AC page and Services page each get their own hero/services/about/faq/cta. Contact page is a single contact section (address/phone/hours/map + form). | claude | site-architecture skill, Phase 1 audience brief.
2026-07-20 | site architecture (Phase 1, reconfirmed) | Operator added a Google Maps/GBP share link and a full brand color palette to Overrides; neither changes the page map (Pages/Services/Primary action/Target search terms fields are still empty). Phase 1 output stands unchanged: Home / AC Installation & Repair / Services / Contact. The Contact page's map embed now has a real URL (https://share.google/TIExO6qEW4nz1Eo8L) instead of a placeholder. | claude | operator edited client.md; re-validated (0 blockers, 10/13 facts confirmed).
2026-07-20 | design direction (Phase 2) | Direction name: "Plainspoken Comfort" — clean, trust-first trades aesthetic; directness over slickness, answering the market's #1 objection (trust=2/10 in the audience brief) rather than competing on corporate polish like Reliance/Enercare. Niche playbook: HVAC/Trades (references/hvac.md - trust+speed, phone-in-header, zero cleverness); 3 vibe images (client/assets-intake/vibe/) confirmed compatible and outrank playbook aesthetics per rule 3. Type pairing: Montserrat Bold (headings) + Montserrat SemiBold (subheads) + Inter Regular (body) - taken from vibe image "ChatGPT Image Jul 20, 2026, 02_32_10 PM.png"'s typography panel (Overrides Brand fonts was empty). Color intent: operator-confirmed palette in Overrides - Deep Navy #0B1F3A (text/authority), HVAC Blue #147DF5 (cool/trust accents), Temp Red #E1261C (CTA/urgency ONLY, kept scarce ~3% per the vibe's own color-balance chart), Ice Blue/Cool White/Pure White (70% light neutral base), Slate Gray body text, Charcoal #121820 for dark bands. Imagery style: photo-real clean daylight equipment/technician photography (matches vibe mockup's AC-unit-outside-a-home shot); no illustration; Photos policy/People in imagery unconfirmed - flag for Phase 4. Layout personality: light, airy "clinical calm" warmed up - generous whitespace, 8-12px rounded cards, soft shadows, icon-led service cards (snowflake/flame/fan/wrench/shield vocabulary, from vibe image 3's icon panel). ONE distinctive element: a thin red-to-navy-to-blue horizontal gradient rule as a section-title underline / card top-accent, literalizing the brand mark's own heat/cool duality (from vibe image 1, the logo) as a recurring signature motif. Motion level: subtle (css-only scroll fades/slides, tap feedback) - matches playbook "avoid animation-heavy" + trust-first mood; Stack animation flag unset, logged claude-proposed. Hero: static/near-static two-tone headline (navy line + red line) over photo, trust-strip icon row under hero (from vibe image 1's mockup) - trust-strip claims limited to what's actually confirmed (24-hour, 5.0 rating) until licensing/certifications are confirmed. Dark band signature: one charcoal/navy full-bleed band reserved for the 24-hour/emergency CTA section (from vibe image 3's "24/7 EMERGENCY SERVICE" band), phone number large and tappable. | claude | design-direction skill; client.md Overrides (Brand colors) + 3 vibe images + niche playbook.
2026-07-20 | tokens.css + contrast fix (Phase 2) | Wrote site/shared/tokens.css from the operator's confirmed palette (all hexes kept exactly as given). One derived addition: --color-accent-text (#0F63C4, darker blue, 5.6:1 on --color-bg) for body-sized accent text, since the given HVAC Blue #147DF5 only hits 3.8:1 (fine for icons/large UI/borders, not body text) — see contracts/design-tokens.md v1.2.0. Separately, scripts/generate-style-preview.py's mechanical contrast check flagged --color-bg (page background, #F8FAFC) as failing 4.5:1 when used as on-primary button-label text (4.46:1) — this was the checker testing the wrong semantic pairing (page-bg token standing in for "white button text"), not an actual flaw in the operator's Temp Red. Fixed the checker (and its mock button/motion swatch) to test/use --color-surface (pure white, the correct "on-color text" role) instead — passes at 4.67:1. --color-primary stays exactly #E1261C as the operator specified; nothing about the brand color changed. Self-hosted 3 font files (Inter 400, Montserrat 600/700, latin subset only - no other Languages confirmed) from Google Fonts into site/assets/fonts/ per the file-structure contract. Font pairing (Montserrat headings / Inter body) taken from the vibe reference, not invented. | claude | design-tokens skill; scripts/generate-style-preview.py fix benefits all future client builds using this template, not just this one.
2026-07-20 | hero treatment updated (supersedes prior "static, no video confirmed") | Operator provided 2 client videos at client/assets-intake/ (Hero loop.mp4, Hero Transition.mp4 - both 1916x1080 h264 24fps ~5.04s; kebab-cased to hero-loop.mp4 / hero-transition.mp4 per contracts/asset-slots.md slot grammar) and gave the exact playback spec in-chat: launch playing hero-loop.mp4 looped (idle state); on scroll to the next section, switch to hero-transition.mp4 and play forward once, freeze on last frame; on scroll back up, play hero-transition.mp4 in reverse back to frame 0, then resume hero-loop.mp4 looping. Implemented per hero-media skill as a composite of its named treatments: internal loop-crossfade dip-to-black for hero-loop.mp4's own loop restart (rules/mp4-vs-sequence default; not confirmed seamless footage) + play-once-freeze for the forward transition + reverse-freeze (rAF currentTime stepping, not negative playbackRate) for the reverse - triggered by an IntersectionObserver on the hero section with a simple state machine (loop -> transitioning-fwd -> transitioned -> transitioning-rev -> loop) so rapid scroll back-and-forth can't double-trigger. prefers-reduced-motion: video stays paused at frame 0, no autoplay, muted+playsinline throughout per skill rules 4-6. Updated in preview/layout-preview.html (binding) and client.md Overrides Stack hero-media flag. FLAG for Phase 4: hero-transition.mp4 is 7.8MB, over the 4MB hard cap (hero-loop.mp4 is 3.0MB, within budget) - ingest/performance skill must re-encode it down before it ships; not silently passed. | claude (operator gave the exact spec in-chat; precedence level 1) | hero-media skill; client-provided assets.
2026-07-20 | hero video = full-bleed stage across hero + trust sections | Operator refined the hero spec after approving the layout: the video is the BACKGROUND of both the hero section and the trust band ("hero-stage" wrapper: absolutely-positioned videos + charcoal gradient overlay rgba(18,24,32,.55-.80), content z-indexed above). Hero copy switches to light-on-dark (white h1 17.9:1, Temp Red accent line 3.8:1 = AA large-text only - display sizes only, ice-blue eyebrow, #C7D0DB lede); trust card stays a white surface card over the video. State machine unchanged but observes the stage (not just #hero) and now handles mid-flight direction reversal (scroll back during forward transition reverses from the current frame, and vice versa). Verified live in the preview pane: loop autoplays (declarative autoplay attribute - JS-only play() was blocked in the embedded pane before first interaction), forward transition plays + freezes on scroll-past, reverse + loop-resume on scroll-back. Phase 5 must reproduce this exact machine; poster + reduced-motion (frame 0 static) still required per hero-media rules 4-6. | operator spec (precedence 1), claude implementation | preview/layout-preview.html is the binding reference.
2026-07-20 | copy voice + hero headline (Phase 3) | Creative section empty -> voice from niche playbook (trades: scannable, short sentences, zero cleverness) + audience brief (trust-first, objections pre-answered). Two hero candidates written: (A) plain-trades register "AC Installation & Repair, Done Right the First Time." vs (B) emergency register "Your AC Died. We Answer - Day or Night." Picked A - the audience splits into emergency + planned-replacement segments, and B narrows to only the first; the 24-hour eyebrow line carries the emergency hook instead. Register logged: PLAIN-DIRECT (grade-7, benefit-first, no superlatives without proof) - keep consistent site-wide. Facts policy applied: name/phone/address/hours/services/service-area are [confirmed] and used directly; Google rating stays [PLACEHOLDER] (ambiguous "5.034" string), testimonials stay [PLACEHOLDER] until the 3 candidates are operator-approved (copywriting rule 6), and the differentiation policy claims (no rentals / no commissioned sales / phone-quote-first) are drafted but ship as [PLACEHOLDER] until the operator confirms they are true business policies - they came from market-research positioning, not from the client. | claude | copywriting skill rules 2/2a/3/6.
2026-07-20 | launch sequence + borderless hero stage (operator modifications at layout review) | Three changes to the binding layout, operator-specified and plan-approved: (1) LOADER - site launches with a full-screen charcoal loading screen, logo centered, signature red->navy->blue gradient line animating beneath as progress cue; holds until hero-loop.mp4 canplaythrough with a 2.5s fallback timeout; fades out to reveal the video. (2) HEADER ENTRANCE - header hidden during load; slides down from the top ~0.5s after the loader fully fades. (3) BORDERLESS STAGE - no white surfaces inside the hero stage: trust card replaced by three light-text stats with icons + thin translucent dividers floating directly on the video; hero elements enter in a staggered float-up (eyebrow -> h1 -> lede -> buttons -> trust stats) after the reveal. Reduced-motion: instant appearance, no loader animation, no float-up; loader is additive (page fully readable without JS). Operator chose loader style "logo + gradient line" and header timing "after the reveal" via in-chat question. | operator (precedence 1) | binding for Phase 5; preview updated same day.
2026-07-20 | hero machine deviation from preview (robustness) | site/script.js extends the binding preview's state machine with a play()-rejection fallback: if the forward transition's video.play() is rejected (no user activation in embedded/webview contexts, iOS Low Power Mode), the same rAF currentTime stepper used for reverse drives the forward pass instead. Behavior is otherwise identical to the approved preview. | claude | layout-systems rule 7 requires logging build deviations; found via embedded-pane testing where synthetic scrolls carry no activation.
2026-07-20 | pinned hero stage (operator refinement, supersedes the flowing hero+trust layout) | The hero stage becomes a scroll-pinned 100svh viewport inside a 200svh scroll wrapper: the video background stays LOCKED in place; scrolling translates the hero copy up and out (scroll-linked, window p 0-0.5), triggers the transition video to play forward (time-based, threshold p>0.1, reverse below), then the trust stats translate up from below the viewport into sight (window p 0.45-1). After the wrapper, normal page flow resumes. Header stays transparent while the stage is pinned; is-scrolled (solid white) now triggers only once the stage is fully scrolled through (home script.js overrides main.js's generic scrollY>8 toggle - listener order is deterministic: main.js first, home script last wins). Trust stats no longer part of the launch float-seq (they enter via scroll). Reduced motion: no pinning, no scroll-linking - static stacked layout, transition video untouched. Implemented in site/ only; preview/layout-preview.html remains at the prior approved state with this row as the binding delta (layout-systems rule 7). | operator (precedence 1) | in-chat spec 2026-07-20.
2026-07-20 | trust layer centered + transition speed 2x + reverse timing fix (operator refinement) | Three fixes to the pinned hero stage: (1) trust layer changed from justify-content:flex-end (bottom-anchored, left the upper 2/3 of the video looking like empty background) to justify-content:center, matching the hero layer so trust content reads as genuinely overlaid on the video rather than orphaned at the bottom edge; overlay gradient's middle stop darkened .55->.68 alpha and text-shadow added to trust stat text so centered content stays legible against bright mid-frame footage. (2) Transition video now plays at 2x (TRANS_SPEED constant): native forward playback sets video.playbackRate=2; the rAF fallback/reverse stepper now uses REAL elapsed time (performance timestamp deltas) multiplied by TRANS_SPEED, replacing a fixed 30fps-per-tick assumption that was silently running at whatever the display's actual refresh rate implied (~2x on a 60Hz screen, non-deterministic across devices) — reverse playback is now an accurate, consistent, clearly-visible frame-by-frame reverse of the actual clip, not a device-dependent guess that could look like a snap on high-refresh displays. | operator (precedence 1) | in-chat spec 2026-07-20; site/script.js + site/style.css.
2026-07-20 | reverse-trigger threshold moved + review carousel added (operator refinement) | ROOT CAUSE of "reverse doesn't play when scrolling up": the reverse trigger was set at VIDEO_TRIGGER=0.18 (measured from the top of the 100vh scroll runway) - since the forward video-play is a real-time event (not scroll-scrubbed), by the time a visitor reached the trust content near the bottom of the runway and started scrolling back up, the video sat frozen at its end frame for the entire 100%->18% span and only began reversing in the final 18% sliver, reading as "nothing happens, then it snaps." Fixed with hysteresis thresholds much closer to the hero/trust boundary: FORWARD_TRIGGER=0.38, REVERSE_TRIGGER=0.34 (small dead-zone prevents boundary jitter), HERO_OUT_END=0.35, TRUST_START=0.40. Now reverse has ~62% of the scroll-up gesture to visibly play, verified via real mouse-wheel scroll (not just programmatic scrollTop) after the first fix attempt passed synthetic tests but the operator caught it live - synthetic scrollTop jumps don't reproduce "scroll partway and check" the way real wheel scrolling does; added a real-scroll re-check step for future motion work. Also added: a continuously auto-scrolling (CSS-only marquee, respects reduced-motion) horizontal row of the 3 real confirmed review quotes inside the trust layer, styled as translucent dark chips (not white cards, keeps the borderless-stage rule) - reuses the SAME approved-pending quotes already in the page's Testimonials section, no new/invented content. | operator (precedence 1) | in-chat spec 2026-07-20; caught via real computer-tool mouse-wheel scroll, not synthetic scrollTop.
2026-07-20 | 6 QUESTIONS.md items answered — promoted to confirmed facts | Operator answered in state/QUESTIONS.md directly: (1) Google rating confirmed 5.0 stars / 34 reviews (the "5.034" reading was correct). (2) "Open 24 hours" confirmed as genuine 24/7 emergency dispatch — usable as a headline claim, not just a GBP default field. (3) The 3 real review quotes (sep, Nariman Shab, Matin Akrami) approved for display with first-name attribution, operator specified showing them in the trust-stat carousel (already built there). (4) All three differentiation policies confirmed TRUE: no rental contracts ever; no commissioned salespeople/no pressure upsells; fair quote over the phone before a visit. (5) Domain deferred — temporary subdomain TBD, not blocking. (6) Temp Master confirmed OWNER-OPERATED — unlocks Positioning Opportunity #1 from the Target Audience research (the anti-corporate identity play vs Reliance/Enercare's commissioned-advisor reputation), previously withheld pending confirmation. client.md Overrides + Auto updated; all corresponding [PLACEHOLDER] copy in site/index.html replaced with real copy. Remaining placeholders (disposal fee, diagnostic fee, rebate handling, brands carried, TSSA licence number, response-time commitment) were NOT part of this answer round — moved from informal chat mentions into proper state/QUESTIONS.md rows so they're formally tracked. | operator (precedence 1) | state/QUESTIONS.md direct answers, 2026-07-20.
2026-07-20 | hero transition: video state-machine -> scroll-scrub canvas (operator refinement) | Replaced the real-time play-once-freeze/reverse-freeze video approach for hero-transition with hero-media's scroll-scrub treatment: extracted hero-transition.mp4 to 61 webp frames (ffmpeg fps=12, scale=1440:-2 -> PNG intermediate, since ffmpeg's webp muxer silently produces one animated file instead of a numbered sequence -> Pillow batch-converts to q70 webp, 2.9MB total, well under the 10MB/sequence cap) + manifest.json per contracts/asset-slots.md. site/script.js now draws frames directly from scroll progress (canvas + drawImage, object-fit:cover handles responsive scaling since the canvas intrinsic bitmap stays fixed at manifest resolution) inside the existing SCRUB_START=0.30/SCRUB_END=0.75 progress window - the idle loop (hero-loop.mp4, still a real <video>) crossfades to the canvas at SCRUB_START and back at the same boundary when scrolling up. This makes reverse-on-scroll-up structurally free (an earlier frame index, not a separate rAF reverse stepper) - directly resolves the reverse-feel concern at its root rather than tuning trigger thresholds. hero-transition.mp4 no longer ships (removed from site/assets/video/); SHOPPING_LIST.md and MEDIA_LOG.md updated to reflect the image-sequence treatment. Reduced-motion: frame 0001 only, no preload beyond it, no scroll-link (unchanged principle, now via the scrub-player pattern instead of the old machine). | operator (precedence 1) | in-chat spec 2026-07-20; hero-media skill's scroll-scrub reference + scrub-player.js template.
2026-07-20 | scrub trigger moved to scroll-start + work gallery added (operator spec) | (1) SCRUB_START moved 0.30 -> 0.02: operator specified the crossfade from hero-loop.mp4 to the hero-transition scrub sequence should happen "when scrolled down is detected" (i.e. immediately, not after 30% of the runway) because the transition's first frame is visually identical to the loop, making the swap invisible; SCRUB_END moved 0.75 -> 0.95 so the scrub "finishes the scroll" exactly as the trust layer completes its own entrance (same TRUST_END). (2) Operator supplied 6 real job-site photos (site/assets/images/1-6.webp, pre-existing in the repo, not yet inventoried or used) - resized to a 1000px max edge and re-compressed (webp q76, ~528KB total vs ~975KB original), renamed descriptively (gallery-furnace-install, -ductless-condenser, -wall-mount-ac, -commercial-rooftop, -ductless-indoor-unit, -commercial-ductwork per what's actually depicted - verified by viewing each image, not guessed) and built into a new "Our Work" gallery section on Home between Services and Testimonials (6 items = widow-safe at both 2 and 3 column breakpoints per layout-systems rule 3; explicit breakpoints used, not auto-fit, to guarantee it). Logged in MEDIA_LOG.md as model=client, credits=0. | operator (precedence 1) | in-chat spec 2026-07-20.
2026-07-20 | 6 more QUESTIONS.md items answered — remaining placeholders resolved | Operator answered: disposal (available, arranged separately) -> AC page checklist updated. Diagnostic fee (free, repair priced normally) -> AC page checklist updated. Rebate assistance ("not sure" - genuinely uncertain, NOT a confirmed no) -> copy does not claim either way, rewritten to invite the customer to ask when they call (AC page FAQ + Services page heat-pump paragraph, the latter's placeholder simply removed since the surrounding sentence didn't need the claim). Brands ("the most available HVAC units" - no specific list) -> copy stays generic ("trusted, widely available equipment"), no brand names invented (AC page FAQ). TSSA/gas licence number (operator said no, i.e. decline to display) -> placeholder removed, generic "safe, certified, code-compliant" language kept, no number shown (Services page). Response-time commitment (stay general) -> FAQ rewritten without a specific ETA claim (Home page). All 7 [PLACEHOLDER] markers across the site are now resolved; only the 3 photo slots remain as the sole QA blocker. | operator (precedence 1) | state/QUESTIONS.md direct answers, 2026-07-20.
2026-07-20 | final 3 photo slots resolved | Operator said "approve all" directly in response to being shown exactly 3 named pending slots (services-ac.jpg, ac-install.jpg, ac-repair.jpg) - valid named-slot approval per media-generation rule 1's own example phrasing ("approve all listed"), not a blanket/ambiguous approval. Before generating, reconsidered against media-generation rule 3 (real beats generated): 2 of the 3 slots had a good real-photo match among the 6 client job photos just added, so those were resolved by REUSE, not generation (services-ac.jpg -> gallery-wall-mount-ac.webp; ac-install.jpg -> gallery-ductless-condenser.webp) - added aspect-ratio+object-fit:cover to both image contexts so a portrait source photo displays consistently regardless of the slot's original landscape intent. Only ac-repair.jpg (open condenser, mid-diagnosis) had no real match among the 6 photos, so it was generated via Higgsfield marketing_studio_image (2 credits, cost preflighted first) using the prompt already written in SHOPPING_LIST.md - reviewed the result before accepting (matches brief: no people/text/watermarks, realistic). MEDIA_LOG.md and SHOPPING_LIST.md updated; all 3 slots now [x] filled. | operator (precedence 1) approved; claude chose reuse-over-generation for 2 of 3 per the real-beats-generated rule | media-generation skill rules 1 and 3.
2026-07-20 | scroll-scrub rAF dependency removed — ROOT CAUSE of "still just a video" | Operator reported the hero was "still just a video playing" and asked for proof, not guesses. Empirically verified the shipped script.js via the running site (forced the throttled rAF to run synchronously to bypass the preview pane's rAF suspension, then read real DOM/canvas state): the scrub logic was 100% correct - on scroll the loop video pauses+hides (opacity 1->0, paused false->true) and the canvas shows distinct frames per scroll position (11 positions -> 11 distinct frame signatures, monotonic), reversible (400->720->400 redraws the identical frame), and time-invariant (parked at one scroll pos for 600ms = frame unchanged, i.e. a true scrub not playback). ROOT CAUSE of what the operator saw: the scroll handler gated apply() behind requestAnimationFrame, and rAF is throttled/suspended whenever the page renders hidden - which is exactly what embedded preview panes do (document.hidden=true in both my pane and, almost certainly, the operator's). Result: loop autoplays but apply() never runs -> scrub frozen -> "just a video". FIX: drive apply() directly off the scroll event (guarded by a scrollY-change check; rect read before writes to stay thrash-free), no rAF gate. Re-verified WITHOUT the rAF override in the same hidden pane: canvas now activates and scrubs distinct frames natively (sigs 13.47M->13.87M->17.78M->22.66M across scroll). check.py still 0 fail; console clean. | claude (root-caused + fixed after operator pushback) | rAF suspension in hidden/embedded render contexts is a real cross-environment robustness bug, not just a test artifact.
2026-07-20 | scrub manifest inlined — fixes file:// (double-click) viewing | Operator asked why their LOCAL file doesn't show the scrub but the http preview does. Root cause: the scrub loader read its manifest via fetch('.../manifest.json'), and fetch() of a local file is blocked by the browser under the file:// protocol (CORS: file: scheme). Opening site/index.html by double-click (file://) -> fetch rejects -> caught+swallowed -> manifest null -> scrub.ready() false -> crossfade block never runs -> just the loop video. This ALSO violated file-structure contract rule 2 ("site/ must be openable standalone - double-clicking site/index.html works with no server"). FIX: manifest values (frames/fps/width/height/pattern + frame-base path) are now inline data-* attributes on the <canvas>; the loader reads them synchronously, zero fetch/network. Frame images still load via plain Image().src (relative paths, file://-safe); the site never calls getImageData in production so canvas-taint under file:// is a non-issue. manifest.json stays on disk as the ingest record; the inline attrs mirror it. Verified http path unchanged (canvas sized from attrs, distinct frames per scroll). Could NOT execute the file:// path in the preview pane (it renders file:// as static snapshots, no JS) - definitive file:// confirmation is a real double-click; but the fix removes the sole file://-incompatible API and the remaining code is a strict subset of the proven http path. check.py 0 fail. NOTE for the template: the scrub-player.js reference template uses fetch(manifest) - it should be updated to the inline-attribute pattern so future builds are file://-safe by default (logged as a lint-os follow-up candidate). | claude (root-caused from operator's file-vs-server question) | file-structure contract rule 2 (standalone/no-server) outranks the scrub-player template's fetch pattern.
2026-07-20 | "owner-operated" positioning | Competitor research identified an owner-operated/named-point-of-contact identity as the top unclaimed positioning angle in the GTA HVAC market, but this is not a confirmed Temp Master fact (not in Overrides or the GBP paste). Logged to state/QUESTIONS.md instead of adopting it as copy direction. | claude | CLAUDE.md hard invariant: never invent client facts; unconfirmed facts drive drafts only, never ship unconfirmed.
